[package]
name = "pet-desktop-app"
version = "1.0.0"
description = "小组学习陪伴宠物系统"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[features]
default = []
